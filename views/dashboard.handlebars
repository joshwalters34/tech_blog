{{#if logged_in}}
{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">

    <h2>{{post.title}}</h2>
    <p>{{post.comment_field}}</p>

   
    {{!-- <h2>
            <a href="/project/{{post.id}}">{{project.name}}</a>
    </h2>
        <p>Created by {{project.user.name}} on {{format_date project.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div>
</div> --}}
{{/each}}
{{/if}}

 <nav>
      <button class="btn" id="logout">+ New Post</button>
          <a href="./post"></a>
    </nav>

    